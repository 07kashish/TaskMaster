spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmaster?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: taskmaster_user
    password: taskmaster_pass

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
